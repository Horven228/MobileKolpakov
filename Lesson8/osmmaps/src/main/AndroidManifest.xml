<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--  РАЗРЕШЕНИЕ НА ДОСТУП К ИНТЕРНЕТУ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
        РАЗРЕШЕНИЕ НА ПРОВЕРКУ СОСТОЯНИЯ СЕТИ
        Нужно для определения, есть ли интернет-соединение
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        РАЗРЕШЕНИЕ НА ТОЧНОЕ МЕСТОПОЛОЖЕНИЕ (GPS)
        Нужно для определения текущей позиции пользователя
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
        РАЗРЕШЕНИЕ НА ПРИБЛИЗИТЕЛЬНОЕ МЕСТОПОЛОЖЕНИЕ (WiFi/сотовая вышка)
        Используется, когда GPS недоступен
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
        РАЗРЕШЕНИЕ НА ЗАПИСЬ НА ВНЕШНЕЕ ХРАНИЛИЩЕ
        Нужно для сохранения кэша карт (чтобы не скачивать каждый раз)
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>